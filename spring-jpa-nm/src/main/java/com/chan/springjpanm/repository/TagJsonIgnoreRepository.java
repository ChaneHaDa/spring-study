package com.chan.springjpanm.repository;

import com.chan.springjpanm.model.TagJsonIgnore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TagJsonIgnoreRepository extends JpaRepository<TagJsonIgnore, Long> {
}
