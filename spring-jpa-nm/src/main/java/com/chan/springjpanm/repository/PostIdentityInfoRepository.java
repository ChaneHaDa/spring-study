package com.chan.springjpanm.repository;

import com.chan.springjpanm.model.PostIdentityInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PostIdentityInfoRepository extends JpaRepository<PostIdentityInfo, Long> {
}
