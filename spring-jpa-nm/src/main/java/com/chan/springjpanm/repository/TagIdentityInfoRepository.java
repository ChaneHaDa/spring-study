package com.chan.springjpanm.repository;

import com.chan.springjpanm.model.TagIdentityInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TagIdentityInfoRepository extends JpaRepository<TagIdentityInfo, Long> {
}
