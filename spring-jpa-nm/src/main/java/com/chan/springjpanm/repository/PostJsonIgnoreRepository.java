package com.chan.springjpanm.repository;

import com.chan.springjpanm.model.PostJsonIgnore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PostJsonIgnoreRepository extends JpaRepository<PostJsonIgnore, Long> {
}
